name: 'Trigger tests execution'

on:
  workflow_dispatch:

jobs:
  trigger-tests:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read
    steps:
      - name: Mock test run
        run: |
          echo "Mock test run executing..."
            
