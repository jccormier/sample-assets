name: 'Trigger promotion'

on:
  workflow_call:

jobs:
  trigger-tests:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read
    steps:
      - name: Mock promotion
        run: |
          echo "Mock promotion executing..."
            