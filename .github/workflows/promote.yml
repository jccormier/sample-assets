name: 'Trigger promotion'
run-name: Promoting ${{ inputs.UUID }}

on:
  workflow_dispatch:
    inputs:
      UUID:
        description: 'Distinct ID'
        required: false
        
jobs:
  trigger-tests:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read
    steps:
      - name: Mock promotion
        run: |
          echo "Mock promotion executing..."
            
